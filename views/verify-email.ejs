<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Email Code Verification</title>
    <style>
      /* Reset styles */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      /* Body Styles */
      body {
        font-family: "Arial", sans-serif;
        color: #333;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
      }

      /* Top Navbar */
      .navbar {
        background-color: rgba(0, 123, 255, 0.9);
        padding: 15px 20px;
        color: white;
        text-align: center;
        font-size: 1.2rem;
        font-weight: bold;
      }

      /* Container for the form */
      .container {
        background-color: rgba(0, 123, 255, 0.9); /* Blue background */
        color: white;
        border-radius: 8px;
        padding: 30px;
        width: 80%;
        max-width: 450px;
        margin: 30px auto;
        text-align: center;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      }

      .container h1 {
        font-size: 2.2rem;
        margin-bottom: 20px;
        font-weight: bold;
      }

      .container p {
        font-size: 1.1rem;
        margin-bottom: 20px;
      }

      /* Input field group */
      .input-group {
        margin-bottom: 20px;
      }

      .input-group input {
        width: 100%;
        padding: 15px;
        font-size: 1.2rem;
        border-radius: 8px;
        border: 2px solid #fff;
        outline: none;
        background-color: #fff;
        transition: border-color 0.3s;
        margin-bottom: 10px;
      }

      .input-group input:focus {
        border-color: rgba(0, 123, 255, 1);
        background-color: #e9f1ff;
      }

      /* Button styles */
      .btn-verify {
        background-color: rgba(120, 233, 40, 0.9);
        border: none;
        color: white;
        font-size: 1.2rem;
        padding: 12px 20px;
        border-radius: 8px;
        cursor: pointer;
        width: 100%;
        transition: background-color 0.3s ease;
      }

      .btn-verify:hover {
        background-color: rgba(0, 123, 255, 1);
      }

      .btn-verify:focus {
        outline: none;
      }

      /* Link styles */
      .link {
        color: rgba(201, 255, 3); /* Green color */
        text-decoration: none;
        font-weight: bold;
        margin-top: 20px;
        display: block;
      }

      .link:hover {
        text-decoration: underline;
      }

      /* Responsive design */
      @media (max-width: 600px) {
        .container {
          width: 90%;
          padding: 20px;
        }
      }
    </style>
  </head>
  <body>
    <!-- Top Navbar -->
    <div class="navbar">Verification System</div>

    <!-- Form Container -->
    <div class="container">
      <h1>Email Code Verification</h1>
      <p>Please enter the 6-digit code sent to your email.</p>

      <!-- Verification Code Input -->
      <form action="/verify-code" method="POST">
        <div class="input-group">
          <input
            type="text"
            name="code"
            maxlength="6"
            placeholder="Enter 6-digit code"
            required
          />
        </div>

        <!-- Submit Button -->
        <button type="submit" class="btn-verify">Verify Code</button>
      </form>

      <!-- Link to resend code -->
      <a href="/resend-code" class="link"
        >Didn't receive the code? Resend it.</a
      >
    </div>
  </body>
</html>
