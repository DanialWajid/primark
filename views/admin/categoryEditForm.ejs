<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Product Category</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container mt-5">
      <h1>Edit Product Category</h1>
      <form action="/admin/category/<%= category._id %>" method="POST" enctype="multipart/form-data">
        <div class="mb-3"
          <label for="categoryName" class="form-label">Category Name</label>
          <input
            type="text"
            class="form-control"
            id="categoryName"
            name="categoryName"
            value="<%= category.categoryName %>"  
            placeholder="Enter category name"
            required
          />
        </div>

        <!-- Image  -->
     <div class="mb-3">
  <label for="categoryImage" class="form-label">Current Category Image</label>
  <div>
    <!-- Show the image instead of the image name -->
    <img
      src="<%= category.categoryImage %>"
      alt="Category Image"
      class="img-thumbnail"
      style="max-width: 150px; max-height: 150px;"
    />
  </div>
</div>
 <div class="mb-3">
          <label for="categoryImage" class="form-label">Upload Image</label>
          <input
            type="file"
            class="form-control"
            id="categoryImage"
            name="categoryImage"
            accept="image/*"

          />
        </div>
        <!-- Category Type -->
        <div class="mb-3">
          <label for="type" class="form-label">Category Type</label>
          <select class="form-select" id="type" name="type" required>
            <option value="" disabled>Select category type</option>
            <option value="Men" <%= category.type === 'Men' ? 'selected' : '' %>>Men</option>
            <option value="Women" <%= category.type === 'Women' ? 'selected' : '' %>>Women</option>
            <option value="Kids" <%= category.type === 'Kids' ? 'selected' : '' %>>Kids</option>
            <option value="Click & Collect" <%= category.type === 'Click & Collect' ? 'selected' : '' %>>Click & Collect</option>
            <option value="Home" <%= category.type === 'Home' ? 'selected' : '' %>>Home</option>
            <option value="Baby" <%= category.type === 'Baby' ? 'selected' : '' %>>Baby</option>
          </select>
        </div>

        <!-- Link Name -->
        <div class="mb-3">
          <label for="linkName" class="form-label">Link Name (URL Slug)</label>
          <input
            type="text"
            class="form-control"
            id="linkName"
            name="linkName"
            value="<%= category.linkName %>"  
            placeholder="Enter link name"
            required
          />
        </div>

        <button type="submit" class="btn btn-primary">Update Category</button>
      </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
